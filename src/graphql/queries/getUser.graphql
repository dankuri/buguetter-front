query getUser($userId: Int!) {
    user(id: $userId) {
        id
        name
        date
        follower {
            id
            name
        }
        following {
            id
            name
        }
    }
}
